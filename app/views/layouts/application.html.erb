<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "One V One" %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="One V One">
    <meta name="theme-color" content="#16a34a">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  </head>
  <body class="min-h-screen bg-slate-50 text-slate-900 antialiased pb-20 md:pb-6">
    <%# Top nav - compact on mobile %>
    <header class="sticky top-0 z-20 bg-white/95 backdrop-blur border-b border-slate-200">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 flex items-center justify-between h-14">
        <%= link_to root_path, class: "font-bold text-lg text-slate-900 tracking-tight" do %>
          One V One
        <% end %>
        <nav class="flex items-center gap-3 sm:gap-4">
          <%= link_to "Pools", leagues_path, class: "text-slate-600 hover:text-green-600 font-medium text-sm sm:text-base" %>
          <% if user_signed_in? %>
            <%= link_to "Dashboard", dashboard_path, class: "text-slate-600 hover:text-green-600 font-medium text-sm sm:text-base" %>
            <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "text-slate-500 hover:text-slate-700 text-sm" %>
          <% else %>
            <%= link_to "Sign in", new_user_session_path, class: "text-slate-600 hover:text-green-600 font-medium text-sm" %>
            <%= link_to "Sign up", new_user_registration_path, class: "inline-flex items-center justify-center rounded-xl bg-green-500 hover:bg-green-600 text-white font-semibold px-4 py-2.5 text-sm min-h-[44px] transition-colors" %>
          <% end %>
        </nav>
      </div>
    </header>

    <% if notice.present? %>
      <div class="max-w-4xl mx-auto px-4 sm:px-6 pt-3">
        <p class="notice rounded-xl bg-green-50 border border-green-200 text-green-800 px-4 py-3 text-sm font-medium"><%= notice %></p>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="max-w-4xl mx-auto px-4 sm:px-6 pt-3">
        <p class="alert rounded-xl bg-red-50 border border-red-200 text-red-800 px-4 py-3 text-sm font-medium"><%= alert %></p>
      </div>
    <% end %>

    <main class="max-w-4xl mx-auto px-4 sm:px-6 py-6 sm:py-8">
      <%= yield %>
    </main>
  </body>
</html>
